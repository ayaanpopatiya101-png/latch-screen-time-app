<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuration for LatchAccessibilityService.
  canRetrieveWindowContent="false" is deliberately set — Latch does NOT read
  screen content. It only watches for window-state-changed events to detect
  which app is in the foreground. This must be stated in the Play Console
  Accessibility Usage declaration.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:settingsActivity="com.latch.screentime.LatchShieldActivity" />
